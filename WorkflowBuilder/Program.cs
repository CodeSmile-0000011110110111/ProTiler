namespace WorkflowBuilder
{
	internal class Program
	{
		public static void Main(string[] args)
		{
			// process command line args
			// load template file (test + build, test only)
			// for each project
			// for each platform
			// for each unity version
			// replace project name, platform, unity version
			// write output file with filename project-platform-unityversion
		}
	}
}
